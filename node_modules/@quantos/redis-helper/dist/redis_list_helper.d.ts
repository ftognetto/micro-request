import * as redis from 'redis';
export interface RedisListHelperConfig<T> {
    client?: redis.RedisClient;
    host?: string;
    port?: number;
    prefix?: string;
    keyExtractor: (obj: T) => string;
    ttl?: number;
}
export declare class RedisListHelper<T> {
    private _redis;
    private _getAsync;
    private _setAsync;
    private _lpushAsync;
    private _prefix;
    private _key;
    private _ttl;
    private _cacheDisabled;
    constructor(config: RedisListHelperConfig<T>);
    getCacheds(ids: string[]): Promise<T[]>;
    getCached(id: string): Promise<T | null>;
    setCached(values: T[]): Promise<void>;
    lPush(value: T): Promise<void>;
}
