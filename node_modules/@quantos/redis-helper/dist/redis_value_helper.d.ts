import * as redis from 'redis';
export interface RedisValueHelperConfig<T> {
    client?: redis.RedisClient;
    host?: string;
    port?: number;
    prefix?: string;
    ttl?: number;
}
export declare class RedisValueHelper<T> {
    private _redis;
    private _getAsync;
    private _setAsync;
    private _prefix;
    private _ttl;
    private _cacheDisabled;
    constructor(config: RedisValueHelperConfig<T>);
    getCached(key: string): Promise<T | null>;
    getCacheds(keys: string[]): Promise<T[]>;
    setCached(key: string, value: T): Promise<void>;
}
